        $(document).ready(function(){                  // when document get ready then this js file work
            
            // targeting id using #idname in $()
            // targeting class using .classname in $()
            // targeting tag using tagname in $()

            var a = $('#test1').html();
            console.log(a);
        })
